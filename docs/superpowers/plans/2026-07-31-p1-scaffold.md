# P1 Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize the RetroFit Next.js 19 + TypeScript + Tailwind project with design tokens extracted from the Stitch design system, ready for Phase 2's static component build.

**Architecture:** Standard Next.js App Router layout with a CSS-first Tailwind v4 theme holding the Stitch design tokens (colors, fonts, pixel-border utility). Scaffold is generated by `create-next-app` in a temp dir (the project folder name "RetroFit" is an invalid npm package name due to uppercase), then copied in with the package name fixed to `retrofit`.

**Tech Stack:** Next.js 19 (App Router), TypeScript, Tailwind CSS v4, Lucide icons, `next/font/google` (Anybody, Inter, JetBrains Mono).

## Global Constraints

- No comments in code unless asked
- Commands run from `/home/dannyboi/projects/RetroFit` unless stated otherwise
- One commit per task with the exact message given
- Colors: background `#0c1609`, surface `#0c1609`, surface-bright `#323c2d`, surface-container `#182214`, on-surface `#dae6d0`, on-surface-variant `#baccb0`, primary `#efffe3`, primary-container `#39ff14`, on-primary-container `#107100`, secondary `#ffb4aa`, secondary-container `#c5020b`, tertiary-container `#ffda70`, outline `#85967c`, outline-variant `#3c4b35`
- Fonts: Anybody (headlines), Inter (body), JetBrains Mono (data/labels)
- No env vars or secrets in this phase

---

### Task 1: Scaffold the Next.js app

**Files:**
- Create: entire generated scaffold in `/tmp/opencode/retrofit-tmp/` (create-next-app output)
- Create: `/home/dannyboi/projects/RetroFit/package.json` (renamed) and all scaffold files copied in

**Interfaces:**
- Consumes: nothing
- Produces: a working Next.js app at project root with `npm run dev` / `npm run build` working; package name `retrofit`

- [ ] **Step 1: Generate the scaffold in a temp directory**

Run:
```bash
npx create-next-app@latest /tmp/opencode/retrofit-tmp --ts --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm --yes
```
Expected: exit 0, scaffold generated at `/tmp/opencode/retrofit-tmp`.

- [ ] **Step 2: Copy the scaffold into the project and fix the package name**

Run:
```bash
rsync -a --exclude .git /tmp/opencode/retrofit-tmp/ /home/dannyboi/projects/RetroFit/
```
Then edit `/home/dannyboi/projects/RetroFit/package.json`: change `"name": "retrofit-tmp"` to `"name": "retrofit"`.

- [ ] **Step 3: Verify the scaffold works**

Run:
```bash
npm run build
```
Expected: exit 0, "Compiled successfully" in output.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js app"
```

---

### Task 2: Design tokens from the Stitch design system

**Files:**
- Modify: `app/globals.css` (replace contents)
- Modify: `app/layout.tsx` (fonts + metadata)

**Interfaces:**
- Consumes: Task 1 scaffold
- Produces: CSS variables usable as Tailwind classes: `bg-background`, `text-on-surface`, `border-outline-variant`, `bg-primary-container`, `font-headline`, `font-mono`, `pixel-border` utility

- [ ] **Step 1: Replace `app/globals.css`**

```css
@import "tailwindcss";

@theme {
  --color-background: #0c1609;
  --color-surface: #0c1609;
  --color-surface-bright: #323c2d;
  --color-surface-container: #182214;
  --color-on-surface: #dae6d0;
  --color-on-surface-variant: #baccb0;
  --color-primary: #efffe3;
  --color-primary-container: #39ff14;
  --color-on-primary-container: #107100;
  --color-secondary: #ffb4aa;
  --color-secondary-container: #c5020b;
  --color-tertiary-container: #ffda70;
  --color-outline: #85967c;
  --color-outline-variant: #3c4b35;

  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
  --font-headline: var(--font-anybody), ui-sans-serif, system-ui, sans-serif;
  --font-mono: var(--font-jetbrains), ui-monospace, monospace;
}

@utility pixel-border {
  border: 3px solid var(--color-outline-variant);
  box-shadow:
    -4px 0 0 0 var(--color-surface),
    4px 0 0 0 var(--color-surface),
    0 -4px 0 0 var(--color-surface),
    0 4px 0 0 var(--color-surface);
}

body {
  background-color: var(--color-background);
  color: var(--color-on-surface);
}
```

- [ ] **Step 2: Replace `app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import { Anybody, Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const anybody = Anybody({ subsets: ["latin"], variable: "--font-anybody" });
const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });
const jetbrainsMono = JetBrains_Mono({ subsets: ["latin"], variable: "--font-jetbrains" });

export const metadata: Metadata = {
  title: "RetroFit",
  description: "8-bit calorie and macro tracker",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${anybody.variable} ${inter.variable} ${jetbrainsMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 3: Verify the build**

Run:
```bash
npm run build
```
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add app/globals.css app/layout.tsx
git commit -m "feat: add Stitch design tokens and fonts"
```

---

### Task 3: Folder structure and token demo page

**Files:**
- Create: `components/`, `lib/`, `db/`, `hooks/` (empty dirs, each with a `.gitkeep`)
- Create: `components/pixel-box.tsx`
- Modify: `app/page.tsx` (replace contents)

**Interfaces:**
- Consumes: Task 2 tokens (`pixel-border`, `font-headline`, `font-mono`, colors)
- Produces: `PixelBox` component — a reusable 8-bit bordered card: `PixelBox({ title, children }: { title: string; children: React.ReactNode })` rendering a titled pixel-bordered box. Later tasks use it for every card on screen.

- [ ] **Step 1: Create the folder structure**

Run:
```bash
mkdir -p components lib db hooks
touch components/.gitkeep lib/.gitkeep db/.gitkeep hooks/.gitkeep
```

- [ ] **Step 2: Create `components/pixel-box.tsx`**

```tsx
export default function PixelBox({
  title,
  children,
}: {
  title: string;
  children: React.ReactNode;
}) {
  return (
    <section className="pixel-border bg-surface-container">
      <header className="border-b-2 border-outline-variant px-3 py-2">
        <h2 className="font-mono text-xs font-semibold uppercase tracking-widest text-primary">
          {title}
        </h2>
      </header>
      <div className="p-4">{children}</div>
    </section>
  );
}
```

- [ ] **Step 3: Replace `app/page.tsx` with a token demo**

```tsx
import PixelBox from "@/components/pixel-box";

export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-lg flex-col gap-6 p-6">
      <header>
        <h1 className="font-headline text-3xl font-extrabold tracking-widest text-primary">
          RETROFIT
        </h1>
        <p className="font-mono text-xs text-on-surface-variant">
          SCAFFOLD v0.1 // TOKENS OK
        </p>
      </header>

      <PixelBox title="Calories">
        <div className="mb-1 flex justify-between font-mono text-xs text-on-surface-variant">
          <span>1,245 / 2,000 kcal</span>
          <span>62%</span>
        </div>
        <div className="h-4 w-full bg-surface-bright">
          <div className="h-full w-[62%] bg-primary-container" />
        </div>
      </PixelBox>

      <PixelBox title="Macros">
        <div className="mb-1 flex justify-between font-mono text-xs text-on-surface-variant">
          <span>PROTEIN</span>
          <span>98 / 150g</span>
        </div>
        <div className="h-2 w-full bg-surface-bright">
          <div className="h-full w-[65%] bg-secondary-container" />
        </div>
        <div className="mb-1 mt-3 flex justify-between font-mono text-xs text-on-surface-variant">
          <span>CARBS</span>
          <span>142 / 200g</span>
        </div>
        <div className="h-2 w-full bg-surface-bright">
          <div className="h-full w-[71%] bg-tertiary-container" />
        </div>
      </PixelBox>

      <button className="pixel-border bg-primary-container px-4 py-2 font-mono text-sm font-semibold text-on-primary-container">
        + LOG MANUALLY
      </button>
    </main>
  );
}
```

- [ ] **Step 4: Verify build and live render**

Run:
```bash
npm run build
```
Expected: exit 0.

Run:
```bash
npm run dev & sleep 4 && curl -s http://localhost:3000 | grep -o "RETROFIT"
```
Expected: output `RETROFIT`. Then kill the dev server:
```bash
kill %1
```

- [ ] **Step 5: Commit**

```bash
git add components lib db hooks app/page.tsx
git commit -m "feat: add folder structure and token demo page"
```

---

## Self-Review Notes

- **Spec coverage:** P1 spec items — Next.js 19 + TS + Tailwind + Lucide (lucide-react arrives with scaffold; used from P2), design tokens from Stitch DS, folder structure, one commit per phase ✓
- **Placeholders:** none; every step has exact code or commands
- **Type consistency:** `PixelBox({ title, children })` defined in Task 3 and used in the same task; token class names match the `@theme` block exactly
- **Deliberate deferral:** the token demo page is replaced by the real Home Dashboard in P2 (by design — gives a reviewable artifact)
